# Подготовка данных и офлайн-оценка baseline (без GPU).
# scipy нужен измерительному ядру dxaqc и ранжированию признаков — версия та же,
# что в backend/requirements.txt, иначе результаты сервиса и оценки разойдутся.
pydicom==3.0.2
numpy==2.4.6
scipy==1.17.1
pillow==12.3.0
openpyxl==3.1.5
# снятие разметки сканера с открытого набора Arak (dxa.arak)
opencv-python-headless==4.13.0.92
